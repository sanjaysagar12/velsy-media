---
// Testimonials component
// Client Projects component
const projects = [
  {
    name: "Sastika Solutions",
    position: "Manpower & Overseas Education Agency",
    image: "https://sastika.eu/logo.svg",
    content:
      "We helped Sastika Solutions build a strong digital presence to support their recruitment services and overseas education programs. From website design to SEO, we created a platform that speaks to students and job seekers across Europe and beyond.",
    },
  {
    name: "MKM Group",
    position: "Logistics & Leather Manufacturing",
    image: "https://yourdomain.com/path-to-mkm-logo-or-image.jpg", // replace with actual image
    content:
      "For MKM Group, we built a professional website and brand identity that reflects their presence in logistics and leather manufacturing. Our team also supported their digital outreach with tailored content and creative assets.",
    },
  {
    name: "Healthy Bake",
    position: "Organic & Health Food Brand",
    image: "/healthu-bakers-logo.jpg", // replace with actual image
    content:
      "We managed Healthy Bake's branding and marketing — including social media creatives and digital campaigns — to help them reach health-conscious customers.",
  },
];

---

<section class="testimonials-section" id="clients">
  <div class="section-title">
    <h2>Client Projects</h2>
    <p>Here’s a glimpse of what we’ve done for some of our amazing clients.</p>
  </div>

  <div class="testimonials-grid">
    {
      projects.map((project) => (
        <div class="testimonial-card">
          <div class="testimonial-header">
            <img
              src={project.image}
              alt={project.name}
              class="testimonial-pic"
            />
            <div class="testimonial-info">
              <h4>{project.name}</h4>
              <p>{project.position}</p>
            </div>
          </div>
          <div class="testimonial-content">{project.content}</div>
        </div>
      ))
    }
  </div>

</section>

<style>
  /* Testimonials */
  .testimonials-section {
    padding: 100px 0;
    position: relative;
  }

  .section-title {
    text-align: center;
    margin-bottom: 80px;
  }

  .section-title h2 {
    font-family: "Montserrat", sans-serif;
    font-size: 3.5rem;
    font-weight: 700;
    margin-bottom: 20px;
    background: linear-gradient(90deg, var(--primary), var(--secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .section-title p {
    color: var(--text-light);
    max-width: 700px;
    margin: 0 auto;
    font-size: 1.2rem;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 30px;
    margin-top: 50px;
  }

  .testimonial-card {
    background: var(--glass);
    border-radius: 20px;
    padding: 30px;
    border: 1px solid rgba(255, 107, 53, 0.1);
    backdrop-filter: blur(10px);
    transition: var(--transition);
    box-shadow: var(--shadow);
  }

  .testimonial-card:hover {
    transform: translateY(-10px);
    box-shadow: var(--shadow-hover);
  }

  .testimonial-header {
    display: flex;
    align-items: center;
    margin-bottom: 20px;
  }

  .testimonial-pic {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    object-fit: cover;
    margin-right: 15px;
    border: 2px solid var(--primary);
  }

  .testimonial-info h4 {
    font-size: 1.2rem;
    color: var(--text-dark);
    margin-bottom: 5px;
  }

  .testimonial-info p {
    color: var(--text-light);
    font-size: 0.9rem;
  }

  .testimonial-content {
    color: var(--text-light);
    font-style: italic;
    line-height: 1.7;
  }
</style>
